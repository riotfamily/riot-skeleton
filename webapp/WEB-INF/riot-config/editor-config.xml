<?xml version="1.0" encoding="UTF-8"?>
<editor-config
	xmlns="http://www.riotfamily.org/schema/riot/editor-config"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.riotfamily.org/schema/riot/editor-config
		http://www.riotfamily.org/schema/riot/editor-config-6.5.xsd">

	<!-- Use this setup if you have only one Locale ... -->
	<tree id="sitemap" list-ref="pages" icon="sitemap">
		<form form-ref="page" label-property="pathComponent" />
	</tree>
	
	<!-- and this one if you have a multi-lingual website ... -->
	<!-- 
	<list id="sitemap" list-ref="site-chooser" icon="sitemap">
		<tree list-ref="pages">
			<form form-ref="page" label-property="pathComponent" />
		</tree>
	</list>
    -->
 
	<!-- ... or this if you want to manage mutiple sites -->
	<!--
	<list id="sitemap" list-ref="sites" icon="sitemap">
		<list list-ref="locales">
			<tree list-ref="pages-i18n">
				<form form-ref="page" label-property="pathComponent" />
			</tree>
		</list>
	</list>
	-->

	<custom name="preview" url="/" target="_blank" icon="external" />

</editor-config>